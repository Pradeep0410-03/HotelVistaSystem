{
  "name": "vista-booking",
  "version": "1.0.0",
  "private": true,
  "description": "Vista Booking category frontend with a Spring booking API",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.cjs",
    "build:css": "tailwindcss -i frontend/styles/input.css -o frontend/styles/vista.css --minify"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3",
    "tailwindcss": "4.3.3"
  }
}
